/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /** Hide the floating Next.js dev tools “N” button (dev only). */
  devIndicators: false
};

module.exports = nextConfig;

